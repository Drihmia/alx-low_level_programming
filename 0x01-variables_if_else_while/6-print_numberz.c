#include <stdio.h>

/**
 * main - integer
 * Return: 0
 */

int main(void)
{
	int i;

	/* for - looping fron equvalents of a to z iin ASCII */
	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar(10);

	return (0);
}
