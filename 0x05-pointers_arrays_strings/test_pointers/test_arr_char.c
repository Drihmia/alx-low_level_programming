#include <stdio.h>

int main(void) {
	// Declare an array of characters and initialize it with a string literal
	char str[] = "Hello, world!";

	// Print the string
	printf("%s\n", str);

	return 0;
}
